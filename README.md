# 🏰 CastleKeeper

**Telegram bot — castle keeper**  
Ask it about a castle, and it will answer like a real gatekeeper!

---

## ⚔️ About

CastleKeeper is an ecosystem for managing a database of medieval castles through Telegram.  
You message the bot, and it returns a list of strongholds from a real PostgreSQL database.  
Add a new castle via REST API — and the bot is ready to tell its story right away.

The project was born from a desire to learn Spring Boot, Docker, and clean architecture — all wrapped in a castle theme.

---

## 🏗 Architecture

| Component          | Technology                       | Description                                   |
|--------------------|----------------------------------|-----------------------------------------------|
| **Telegram Bot**   | Java 25 (LTS), Maven             | Processes commands, calls REST API            |
| **REST API**       | Spring Boot 3, Java 17, Flyway   | CRUD for castles, authors, types, materials   |
| **Database**       | PostgreSQL 16 (Docker)           | Stores all entities with FK relationships     |
| **Documentation**  | Obsidian (root vault)            | Notes, architecture, API contracts, canvas    |
| **Infrastructure** | Docker Compose                   | One-command PostgreSQL setup                  |

### Data flow

```
Telegram (user) → Bot (Java 25) → HTTP → Server (Spring Boot 3, Java 17) → PostgreSQL
```

> 💡 Why different Java versions? The bot uses JDK 25 as LTS, the server uses Java 17 for maximum compatibility with Spring Boot 3.x.

---

## 📂 Project Structure

```
castlekeeper/
├── .github/workflows/         # CI: auto-generate DB schema docs via tbls
├── .obsidian/                 # Obsidian vault config (root-level vault)
├── bot/                       # Telegram bot (Java 25 + Maven)
│   ├── pom.xml
│   └── src/main/java/org/kaelth4s/castlekeeper/
├── server/                    # REST API (Spring Boot 3, Java 17, Maven)
│   ├── pom.xml
│   └── src/main/
│       ├── java/.../server/
│       │   ├── controller/    # REST endpoints
│       │   ├── service/       # Business logic + @Transactional
│       │   ├── repository/    # Spring Data JPA interfaces
│       │   ├── model/         # @Entity classes
│       │   ├── dto/           # ApiError response POJO
│       │   └── exception/     # GlobalExceptionHandler + custom exceptions
│       └── resources/
│           ├── application.properties
│           ├── api-docs.json  # OpenAPI 3.0 specification
│           └── db/migration/  # Flyway SQL migrations (V1–V5)
├── docs/                      # Project documentation
│   ├── architecture.md        # System overview, component diagram
│   ├── bot-commands.md        # Full command reference with dialog flows
│   └── database-schema/       # ERD + per-table docs (auto-generated by tbls)
├── notes/                     # Obsidian working notes & plans
│   ├── welcome.md
│   ├── summary.canvas
│   ├── server.md
│   ├── bot/
│   └── database/
├── docker-compose.yml         # PostgreSQL 16 (Docker Compose)
├── .env.example               # Environment variables template
├── .tbls.yml                  # tbls config for DB doc generation
└── README.md
```

---

## 🗄 Database Schema

5 entities with FK relationships — schema managed by **Flyway migrations** in `server/src/main/resources/db/migration/`.

```
author_type ──< author ──< castle >── material
                 │            │
                 └──< reconstruction
```

| Table | Description | Key Columns |
|-------|-------------|-------------|
| `author_type` | Author category (builder, chronicler, etc.) | `id` PK, `name` UNIQUE, `description` |
| `author` | Castle author / builder | `id` PK, `name`, `author_type_id` FK |
| `material` | Building material | `id` PK, `name` UNIQUE |
| `castle` | The castle itself | `id` PK, `name`, `description`, `author_id` FK, `built_year`, `destroyed_year`, `height_m`, `material_id` FK |
| `reconstruction` | Reconstruction event | `id` PK, `castle_id` FK, `author_id` FK, `reconstruction_year` |

> 📖 Full schema docs: [`docs/database-schema/`](docs/database-schema/README.md) — auto-generated from the live database via [`tbls`](https://github.com/k1LoW/tbls) in CI.

---

## 🤖 Bot Commands

The bot responds with a medieval narrative style. Selection-based actions use **inline keyboards** (5 items per page, ◀ ▶ pagination) — no manual ID entry.

### 🏰 Castles
| Command / Action | Description |
|------------------|-------------|
| `/castles` | List all castles (text output) |
| `/castlemenu` → 👁️ Select | View castle via inline keyboard picker |
| `/random` | Random castle (text output) |
| `/addcastle` | 7-step wizard: name → desc → author → years → height → material → confirm |
| `/castlemenu` → ✒️ Edit | Pick castle → pick field → enter value / pick FK → confirm |
| `/castlemenu` → 💥 Delete | Pick castle → confirm ✅/❌ |

### 👥 Authors
| Command / Action | Description |
|------------------|-------------|
| `/authors` | List all authors (text output) |
| `/authormenu` → 👁️ Select | View author via inline keyboard picker |
| `/addauthor` | Wizard: name → type picker → confirm |
| `/authormenu` → ✒️ Edit | Pick author → pick field → enter value / pick type → confirm |
| `/authormenu` → 💥 Delete | Pick author → confirm ✅/❌ |

### 🏷️ Author Types
| Command / Action | Description |
|------------------|-------------|
| `/author_types` | List all types (text output) |
| `/addauthor_type` | Wizard: name → description → confirm |
| `/atypemenu` → ✒️ Edit | Pick type → pick field → enter value → confirm |
| `/atypemenu` → 💥 Delete | Pick type → confirm ✅/❌ |

### 🧱 Materials
| Command / Action | Description |
|------------------|-------------|
| `/materials` | List all materials (text output) |
| `/materialmenu` → 👁️ Select | View material via inline keyboard picker |
| `/addmaterial` | Enter name → confirm |
| `/materialmenu` → ✒️ Edit | Pick material → enter new name → confirm |
| `/materialmenu` → 💥 Delete | Pick material → confirm ✅/❌ |

### 🏰 Navigation
| Command | Description |
|---------|-------------|
| `/start` | Castle gates — welcome screen |
| `/menu` | Main hall — three wings + help |
| `/help` | Foliant with all commands |

> 📖 Full command reference with dialog flows: [`docs/bot-commands.md`](docs/bot-commands.md)  
> 🗺️ Visual dialog flow: `notes/bot/dialogs.canvas` (Obsidian Canvas)

---

## 🔌 API Endpoints

**Base URL:** `http://localhost:${SERVER_PORT:8080}/api/` (local) · `https://${DOMAIN}/api` (production)

> 📄 Machine-readable specification: [`server/src/main/resources/api-docs.json`](server/src/main/resources/api-docs.json) (OpenAPI 3.0)  
> 🖥️ Interactive explorer: `http://localhost:${SERVER_PORT:8080}/swagger-ui.html`
>
> 💡 **DTO Pattern:** All endpoints use dedicated Request/Response DTOs. POST/PUT accept FK fields as plain IDs (`authorTypeId`, `materialId`). GET returns nested objects with full display data. See [`docs/architecture.md`](docs/architecture.md) for details.

### 🏰 Castles
| Method | Endpoint | Description | Status |
|--------|----------|-------------|--------|
| GET | `/api/castles` | List all castles | 200 |
| GET | `/api/castles/random` | Random castle | 200 |
| GET | `/api/castles/{id}` | Castle by ID | 200 / 404 |
| POST | `/api/castles` | Create castle | 201 / 400 |
| PUT | `/api/castles/{id}` | Update castle | 200 / 400 / 404 |
| DELETE | `/api/castles/{id}` | Delete castle | 204 / 404 |

### 👥 Authors
| Method | Endpoint | Description | Status |
|--------|----------|-------------|--------|
| GET | `/api/authors` | List all authors | 200 |
| GET | `/api/authors/{id}` | Author by ID | 200 / 404 |
| POST | `/api/authors` | Create author | 201 / 400 |
| PUT | `/api/authors/{id}` | Update author | 200 / 400 / 404 |
| DELETE | `/api/authors/{id}` | Delete author | 204 / 404 |

### 🏷️ Author Types
| Method | Endpoint | Description | Status |
|--------|----------|-------------|--------|
| GET | `/api/author-types` | List all types | 200 |
| GET | `/api/author-types/{id}` | Type by ID | 200 / 404 |
| POST | `/api/author-types` | Create type | 201 / 400 |
| PUT | `/api/author-types/{id}` | Update type | 200 / 400 / 404 |
| DELETE | `/api/author-types/{id}` | Delete type | 204 / 404 |

### 🧱 Materials
| Method | Endpoint | Description | Status |
|--------|----------|-------------|--------|
| GET | `/api/materials` | List all materials | 200 |
| GET | `/api/materials/{id}` | Material by ID | 200 / 404 |
| POST | `/api/materials` | Create material | 201 / 400 |
| PUT | `/api/materials/{id}` | Update material | 200 / 400 / 404 |
| DELETE | `/api/materials/{id}` | Delete material | 204 / 404 |

### Example Requests (POST)

```json
// POST /api/materials — simplest
{"name": "Гранит"}

// POST /api/authors — FK as ID
{"name": "Генрих", "authorTypeId": 1}

// POST /api/castles — multiple FK IDs
{"name": "Нойшванштайн", "authorId": 3, "materialId": 2, "builtYear": 1869}
```

> 📄 All schemas: [Swagger UI](http://localhost:8080/swagger-ui.html) · Machine-readable: [`api-docs.json`](server/src/main/resources/api-docs.json)

---

## 📖 Documentation

| Document | Purpose |
|----------|---------|
| [`docs/architecture.md`](docs/architecture.md) | System overview: component diagram, request lifecycle, design decisions |
| [`docs/bot-commands.md`](docs/bot-commands.md) | Full command reference with dialog state machine |
| [`docs/database-schema/`](docs/database-schema/README.md) | ERD + per-table documentation (CI auto-generated) |
| [`server/src/main/resources/api-docs.json`](server/src/main/resources/api-docs.json) | OpenAPI 3.0 machine-readable spec |
| `http://localhost:8080/swagger-ui.html` | Interactive API explorer (when server is running) |
| `notes/bot/dialogs.canvas` | Visual dialog flow (open in Obsidian) |

---

## 📄 License

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

This project is distributed under the MIT license.  
Full text: [LICENSE](LICENSE)

---

## 👤 Author

**kaelth4s** (GitHub) — keeper of this digital castle.  
Project started in 2026 and continues to be built, stone by stone.
